    // useEffect(() => {
    //     async function api() {
    //         const track_url = 'https://api.spotify.com/v1/tracks/11dFghVXANMlKmJXsNCbNl'
    //         let data = await getResponse(track_url, token);
    //         setImg(data.data.album.images[0].url);

            // const newTrack = document.createElement("div");
            // newTrack.className = 'track-box';

            // const albumImg = document.createElement("img");
            // albumImg.className = 'album-img';
            // albumImg.src = data.data.album.images[0].url;
            // newTrack.append(albumImg);

            // const trackName = document.createElement("h3");
            // trackName.className = 'name';
            // trackName.textContent = data.data.name;
            // newTrack.append(trackName);

            // const artistName = document.createElement("p");
            // artistName.className = 'name';
            // artistName.textContent = data.data.artists[0].name;
            // newTrack.append(artistName);

            // let elem = document.getElementById('playlist-box');
            // elem?.append(newTrack);
            // console.log(data.data.name);
    //     }
    //     api();
    // }, []);

    // async function api() {
    //     const track_url = 'https://api.spotify.com/v1/tracks/11dFghVXANMlKmJXsNCbNl'
    //     let data = await getResponse(track_url, token);
        
    //     return await data;
    // }
    // let data = api();
    // console.log(data);


    